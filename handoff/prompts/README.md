# Reusable prompts (copy into Cursor / Codex)

Each `.md` file is a **task template**. Replace bracketed placeholders before sending.

Conventions:

- **Scope:** say “do not change desktop” / “do not touch admin” when needed.
- **Verify:** mention files to grep (`mp-calendar.js`, `v1-main.css`, etc.).
