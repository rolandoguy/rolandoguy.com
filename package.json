{
  "name": "rolandoguy-site_1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "rebuild:public": "node scripts/build-public-safe.js",
    "build": "node scripts/build-public-safe.js",
    "check:public-smoke": "node scripts/check-public-smoke.js",
    "test": "npm run check:public-smoke",
    "build:epk-bios": "node scripts/build-epk-bios.js",
    "build:mp-locales": "node scripts/build-mp-locales.js",
    "build:hero-config": "node scripts/build-hero-config.js",
    "build:mp-home": "node scripts/build-mp-home.js",
    "build:biography": "node scripts/build-biography.js",
    "build:repertoire": "node scripts/build-repertoire.js",
    "build:calendar": "node scripts/build-calendar.js",
    "build:press": "node scripts/build-press.js",
    "build:media": "node scripts/build-media.js",
    "build:contact": "node scripts/build-contact.js",
    "build:prerender-public": "node scripts/prerender-public-html.js",
    "build:public-data": "npm run build:epk-bios && npm run build:mp-locales && npm run build:hero-config && npm run build:mp-home && npm run build:biography && npm run build:repertoire && npm run build:calendar && npm run build:press && npm run build:media && npm run build:contact",
    "build:public": "node scripts/build-public-safe.js",
    "build:public:quick": "npm run build:prerender-public",
    "deploy:prepare": "node scripts/build-public-safe.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pdfmake": "^0.2.12"
  }
}
